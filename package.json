{
  "name": "strata",
  "version": "0.1.0",
  "private": true,
  "description": "A layered graph workspace for Obsidian",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs"
  },
  "dependencies": {
    "cytoscape": "^3.31.0"
  },
  "devDependencies": {
    "@types/cytoscape": "^3.21.9",
    "@types/node": "^22.10.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "latest",
    "typescript": "^5.7.0"
  }
}
